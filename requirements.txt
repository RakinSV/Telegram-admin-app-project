# --- Telegram ---
telethon==1.36.0              # чтение каналов (юзер-сессия)
python-telegram-bot==21.6     # постинг и модерация (Bot API), async

# --- БД ---
SQLAlchemy==2.0.35
alembic==1.13.3

# --- Конфиг ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.2.2          # CVE-2026-28684 fixed in 1.2.2

# --- Рерайт (любой OpenAI-совместимый API) ---
openai==1.51.0
httpx==0.27.2

# --- Планировщик ---
APScheduler==3.10.4

# --- Тесты ---
pytest==9.0.3                 # CVE-2025-71176 fixed in 9.0.3
pytest-asyncio==1.4.0          # 0.24.0 не совместим с pytest 9 (requires pytest<9)

# --- Веб-админка (Фаза 5, F23) ---
fastapi==0.138.2
uvicorn[standard]==0.49.0
itsdangerous==2.2.0           # подпись cookie-сессий (через Starlette SessionMiddleware)
jinja2==3.1.6                 # серверный рендеринг шаблонов
python-multipart==0.0.32      # form/file data (форма setup-визарда)
cryptography==49.0.0          # Fernet — шифрование секретов at rest
passlib[argon2]==1.7.4        # хэширование пароля админа
argon2-cffi==25.1.0
sse-starlette==3.4.5          # живые логи (Server-Sent Events)

# --- Guardian: бот-модератор группового чата (guardian/) ---
aiogram==3.29.0                # FSM для многошаговой верификации при вступлении
