"""Публикация лонгридов на telegra.ph (собственный сервис Telegram).

Зачем: пост в канале ограничен 4096 символами, а подпись к картинке — 1024,
и код-блоки в них не отрендерить (см. `telegram/publisher.py` про
осознанно выключенный parse_mode). Статья на Telegraph — 64 КБ, с
`<pre>`/`<code>` и картинками между абзацами, и Telegram открывает её через
Instant View прямо в приложении.

Ключ не нужен: `createAccount` выдаёт access_token сразу, без регистрации.
"""
